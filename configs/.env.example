# ----------------------------------------------------------------------
# MongoDB Connection Settings
# ----------------------------------------------------------------------
# (Required) The connection URL for your MongoDB instance.
# For local Docker setup, this should point to the 'mongo-cli' service.
MONGO_URL=mongodb://admin:password@localhost:27017/?replicaSet=rs0&authSource=admin

# (Required) The name of the database to use for migrations.
MONGO_DATABASE=test_db

# (Optional) Credentials for MongoDB authentication.
# Leave blank if authentication is disabled in your local setup.
MONGO_USERNAME=admin
MONGO_PASSWORD=password

# ----------------------------------------------------------------------
# Migration Settings
# ----------------------------------------------------------------------
# (Optional) The directory where your migration files are located.
MIGRATIONS_PATH=./migrations

# (Optional) The name of the collection used to track migration history.
MIGRATIONS_COLLECTION=schema_migrations

# ----------------------------------------------------------------------
# Connection Pool & Timeout Settings
# ----------------------------------------------------------------------
# (Optional) Maximum number of connections in the connection pool.
MONGO_MAX_POOL_SIZE=10

# (Optional) Minimum number of connections in the connection pool.
MONGO_MIN_POOL_SIZE=1

# (Optional) Maximum time (in seconds) that a connection can remain idle.
MONGO_MAX_IDLE_TIME=300

# (Optional) Timeout (in seconds) for database operations.
MONGO_TIMEOUT=60

# ----------------------------------------------------------------------
# AI Analysis Settings (Optional)
# ----------------------------------------------------------------------
# Set to 'true' to enable AI-powered analysis of migrations.
AI_ENABLED=false

# The AI provider to use. Supported: "openai", "gemini", "claude".
AI_PROVIDER=openai

# API key for your chosen AI provider.
OPENAI_API_KEY="your_openai_api_key_here"
GEMINI_API_KEY="your_gemini_api_key_here"
CLAUDE_API_KEY="your_claude_api_key_here"

# ----------------------------------------------------------------------
# Google Docs Integration Settings (Optional)
# ----------------------------------------------------------------------
# Set to 'true' to enable exporting analysis reports to Google Docs.
GOOGLE_DOCS_ENABLED=false

# Path to your Google Cloud service account credentials file.
# GOOGLE_CREDENTIALS_PATH="./credentials.json"

# Alternatively, you can paste the JSON content directly.
# GOOGLE_CREDENTIALS_JSON=""

# The ID of the Google Drive folder where documents should be created.
# GOOGLE_DRIVE_FOLDER_ID=""
