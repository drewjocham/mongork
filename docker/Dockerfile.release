# syntax=docker/dockerfile:1.7
FROM alpine:3.23

RUN apk --no-cache add ca-certificates tzdata \
    && adduser -D -s /bin/sh migration

WORKDIR /app

COPY mongo-tool /usr/local/bin/mongo-tool

USER migration

ENTRYPOINT ["/usr/local/bin/mongo-tool"]
CMD ["--help"]
